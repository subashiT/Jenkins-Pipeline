Jenkins CI/CD Practice
This project implements a Continuous Integration and Continuous Deployment (CI/CD) pipeline using Jenkins to automate the build, test, and deployment processes for a Python-based application. The pipeline clones the source code from a GitHub repository, builds the application, runs tests by executing the Python app, and deploys it to a production environment.
